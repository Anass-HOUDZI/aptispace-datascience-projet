# 🧠 Jalon 2 : Modélisation Prédictive & Apprentissage (Classification Hybride)

Dans ce notebook du **Jalon 2**, l'objectif est d'implémenter un pipeline complet pour prédire la conversion d'un client (`Response`). 
Notre problématique métier n'est pas une régression continue, mais une **classification binaire fortement déséquilibrée** (très peu de clients achètent par rapport au volume total).

### 1. Préparation et Exécution du Pipeline Hybride

Afin de corriger le déséquilibre massif de nos classes, nous utilisons `XGBClassifier` avec le paramètre mathématique `scale_pos_weight`. De plus, nous enrichissons nos données tabulaires avec des caractéristiques latentes extraites par notre réseau de neurones CNN 1D (Feature Extraction).

```{{python}}
import os
import sys

# 1. Remonter à la racine du projet pour trouver le dossier 'src' et 'data'
racine_projet = os.path.abspath('../../')
if racine_projet not in sys.path:
    sys.path.append(racine_projet)

from src.model_marketing import train_hybrid_pipeline

# 2. Cibler le fichier Parquet nettoyé (Phase 2)
DATA_PATH = os.path.join(racine_projet, "data", "processed", "marketing_clean.parquet")

print("Démarrage de la modélisation hybride (XGBoost + CNN 1D)...")

# 3. Entraînement et évaluation
xgb_final, cnn_extractor = train_hybrid_pipeline(DATA_PATH)